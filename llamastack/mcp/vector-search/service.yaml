apiVersion: v1
kind: Service
metadata:
  name: vector-search-mcp
  namespace: NAMESPACE_PLACEHOLDER
  labels:
    app: vector-search-mcp
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: mcp
  selector:
    app: vector-search-mcp

