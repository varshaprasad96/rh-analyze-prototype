# Kagenti Secrets Configuration
# Minimal configuration for installation

secrets:
  # GitHub credentials (optional for basic install)
  githubUser: ""
  githubToken: ""
  
  # OpenAI API Key - not needed since we use Llama Stack
  openaiApiKey: "not-needed-using-llama-stack"
  
  # Optional credentials
  slackBotToken: ""
  adminSlackBotToken: ""
  quayUser: ""
  quayToken: ""

